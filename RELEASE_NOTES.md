# GPT分析器 1.0.9

- 新增服务器同步：Mac 启动和刷新时从服务器读取历史数据，服务器负责持续轮询。
- 新增首次上传服务器，上传成功后按钮隐藏，服务器拒绝二次初始化，避免多客户端串数据。
- 新增累加成本弹窗，支持日期、备注、金额，并可查看和一键清空累加成本历史。
- 优化合伙结算布局，公式单行铺满右侧空间，蓝色标签保持左侧。
- 账号池选择会本机保存，下次打开沿用上次选择。
- 切换 Tab 时表格平滑滚动到最新一行。
- 增加单实例保护，避免本机同时打开多个 GPT 分析器。
