# 91 2.2.13

- 导入与提现任务移到全局任务中心，切换任意 Tab 后仍保留执行、失败、部分成功和完成状态，并可查看逐项明细。
- 导入任务新增失败平台定向重试，复用首次生成的随机邮箱并在重试前核对已导入账号，成功平台不会重复执行。
- 提现改为读取各 Pixel 实例的实时频控、豁免门槛、余额、待结算申请和历史记录；达到次数上限或已有待结算时自动跳过并显示原因。
- 全部提现支持逐账号修改整数金额和输入 0 主动跳过，首次提现会预留 0.10 元手续费，提交前增加账号数、总额与手续费确认。
- 新增独立提现重试和超时历史核对；拒绝、取消和未知结果不会误报成功或重复冲减成本，Pixel 已受理时显示待结算状态与提现 ID。
- 导入与汇总整理任务改为原子互斥，修复删除后重试的审计状态，并限制失败任务凭据在内存中的保留规模。

# 91 2.2.12

- 账号池管理新增“批量测试并打开共享”，完整扫描全部平台账号、每批 100 个测试，然后无论测试结果都重新打开公共共享。
- 全池恢复按钮放在“汇总导出”旁边，悬浮显示操作说明和扫描、测试、共享进度，完成后逐平台汇总结果。
- 修复 JSON 导入后 PixelAPI 延迟可见或规范化账号名时未打开共享，导入后会重试发现新账号并恢复共享。
- 切换 Tab 时保留账号池导入任务和进度，临时网络或服务错误不再丢失任务 ID。
- 本机未配置 Pixel 管理密钥时，服务器回退请求不再发送空管理密钥请求头。

# 91 2.2.9

- 账号池管理新增“清理限流/保护”和“清理错误账号”，完整扫描全部平台后按每批 100 个删除，并逐平台报告失败或跳过原因。
- 打包版账号列表、额度、测试、编辑、删除和公共共享改为本机直连 Pixel，三个节点竞速并在故障时自动回退服务器。
- 账号表默认每页 100 条；限流和限额保护账号不再请求 5h/7d 额度，直接显示已耗尽。
- 永久排除指定退役账号，后续配置、余额、号池、提现和通知任务均不再读取或操作该账号。
- 适配 PixelAPI 1.2.31 的新公共共享协议，改用 `external-placement:convert-batch` 将账号转入公共池。
- 随机并发数 3-10 改为先单独更新，再打开公共共享，不再向 PixelAPI 已禁止的批量接口写入 `share_mode`。
- 操作后统一回查账号的公共状态和实际并发数，避免把接口接收请求误报为共享成功。

# 91 2.2.8

- 账号池管理新增“一键打开共享”，可跨七个平台扫描全部账号并批量尝试开启公共共享。
- 公共共享优先调用 PixelAPI 管理端批量更新接口，失败再回退用户侧接口，并用账号列表回查确认真实 public 状态。
- 公共共享并发改为每个账号随机设置 3-10，避免所有账号使用同一并发数。
- 批量编辑“设为公共账号池”、导入后自动共享和导入记录重试共享统一使用同一套真实回查逻辑。

# 91 2.2.7

- 成本提现改为每个账号提交成功后立即逐笔冲减成本，失败、跳过和未执行金额不会扣减，服务重启后也不会重复扣减。
- 成本为 0 时不再默认显示 1 元，禁止创建成本提现并提示切换到全部提现。
- 提现邮件重新排版，增加逐笔成本冲减、成本历史、提现后汇总及醒目的双方最终结算金额。
- 将余额历史合并到成本计算页，移除重复的成本历史页面和表格，累加成本历史继续通过按钮查看。
- 账号池多平台上传继续保持 30 秒严格串行等待，并新增“加速下一步”；加速仅缩短当前等待，不会并发上传。
- 补充上传加速、严格串行、逐笔成本冲减、零成本拦截和幂等恢复测试。

# 91 2.2.6

- 提现任务完成后发送排版清晰的 HTML 汇总邮件，同时保留纯文本备用版本。
- 邮件完整展示成本历史、七个账号提现明细、执行状态、提现后总成本、总余额和折后利润。
- 将实际到账归属和双方结算转账移到邮件最底部，并用醒目样式重点突出关键金额。
- 失败或暂停任务继续保留完整执行痕迹，已提交账号不会因服务重启而重复提现。

# 91 2.2.5

- 应用展示名、窗口菜单、安装包和发布产物统一为 `91`。
- 新增成本提现与全部提现，七个账号由服务端持久化队列严格串行执行，支持进度查看、等待倒计时和安全加速。
- 新增提现记录与账号明细；任务完成后自动结清已收回的成本快照，并保留成本日期、金额、备注和录入时间。
- 全部提现按整数余额提交，并按“成本 + 利润 60% / 40%”生成双方结算与转账金额。
- 提现完成后向固定双方发送汇总邮件，包含成本明细、账号状态、提现后总成本、总余额和折后利润。
- 账号池导入记录支持仅删除历史记录，或删除本次导入账号并保留完整执行结果。
- 移除掉号预警入口和相关邮件功能，优化成本计算页与提现弹窗布局。

# 91 2.2.4

- 支持一次选择多个 JSON 文件批量导入，并记录每个源文件名和各平台生成的文件名。
- 修复 PixelAPI 凭据清洗，移除平台不允许的认证元数据字段。
- 修复公共共享结果误报，统一使用 PixelAPI 批量更新接口，公共共享并发固定为 10。
- 公共共享重试结果会同步保存到导入记录，支持重启后继续查看准确状态。

# 91 2.2.3

- 新增 JSON 导入记录，永久保存导入时间、文件名、实际选择的平台和生成的随机邮箱，不保存原始凭据。
- 导入记录支持按记录一键删除，只精确匹配本次导入的随机邮箱；改名、缺失、重复和失败账号会报告并支持重试。
- 账号池管理默认分页改为 50 条，顶部操作按钮按选择 JSON、上传到平台、导入记录、汇总导出排列并补充图标。
- 应用产品名、窗口标题和打包产物统一为 91。

# 91 2.1.2

- 杀号热力格改为直接显示每小时账号删除数；新增错误以红点提示，不再把两种数量混为一个数字。
- 悬浮时即时显示时段详情，点击后可固定查看新增错误、删除拆分、样本数、覆盖时长和覆盖率。
- 当前小时按约 5 分钟快照持续累计，明确显示“进行中”和最后快照时间，不需要等到整点。
- 右侧补号时机卡分开显示本小时实时数据与近 7 天相同时段累计，避免口径歧义。
- 当前小时已覆盖至少 10 分钟且集中删除达到号池约 1% 时，补号时机直接升为高风险并写明触发依据。
